const express = require('express')
const router = express.Router()

const authController = require('../controllers/auth_controller')

//router.post('create_user', authController.register)
module.exports = router